/**
 * RuleSet interface for pluggable rule systems
 * Pure TypeScript - NO React dependencies
 * 
 * This file re-exports the RuleSet interface from types.ts for convenience.
 */

export type { RuleSet } from './types';
